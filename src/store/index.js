import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = {
    bbb: 'xl'
}
const mutations = {}
const getters = {}
const actions = {}
const modules = {}

export default new Vuex.Store({
    state,
    getters,
    mutations,
    actions,
    modules
})
